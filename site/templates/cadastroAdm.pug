body
	| Foto:
	img(id="imagemCadastro" alt="fotoAdministrador" width="136px" height="154px")
	br
	br
	br
	form(style="clear: both")
		| Alterar Foto:
		input(id="inputImagemCadastro" type="file" onchange="alterouImagemCadastro(this)")
		br
		br
		| Nome:
		input(id="nomeAdm" type="text" name="cadastroAdm" value=a.nome)
		br
		br
		| Telefone:
		input(id="telefoneAdm" type="tel" name="telefoneAdm" value=a.telefone)
		br
		br
		| E-mail:
		input(id="emailAdm" type="email" name="emailAdm" value=a.email)
		br
		br
		br
		if(!proprioCadastro)
			input(id="voltar" type="button" onClick="alterarPagina('listaAdm')" value="Voltar")
			input(id="remover" type="button" onClick="removerCadastro('adms',  this.getAttribute('cod'))" value="Remover Cadastro" cod=a._id)
		input(id="salvar" type="button" onClick="alterarCadastroAdm('adms', this.getAttribute('cod'), true)" value="Salvar" cod=a._id)
	br
